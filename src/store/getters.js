export default {
    allItems(state) {
        return state.items;
    },
    featuredItems(state) {
        return state.items;
    },
    signedUser(state) {
        return state.signedUser;
    }
}
